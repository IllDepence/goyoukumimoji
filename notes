# from \u3300, ord 13056 (㌀), to \u3357, ord 13143 (㍗)
# plus \u1F200, ord 127488 (🈀)

# list of ords
l = [x for x in range(13056, 13143+1)] + [127488]

# list of characters
c = [chr(x) for x in range(13056, 13143+1)] + [chr(127488)]

# decomposition of a character
[chr(int(x, 16)) for x in decomposition(chr(13143)).split(' ')[1:]]

# word list katakata conversion
$ head word_list -n 50000 | kakasi -i utf8 -JK -HK > kk_word_list_50k
